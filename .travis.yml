language: python
sudo: false
python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6
  - pypy

cache:
    directories:
        - $HOME/.hypothesis

env:
  global:
    - HYPOTHESIS_STORAGE_DIRECTORY=$HOME/.hypothesis

install:
  - travis_retry pip install hypothesis pytest-catchlog
  - pip install .

# Run test
script:
  - py.test -vv test.py

notifications:
  email:
    on_success: change
    on_failure: always
